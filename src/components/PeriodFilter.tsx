export function PeriodFilter({ preset, from, to }: { preset: string; from: string; to: string }) {
  return <form className="filters" method="get"><div className="preset-links"><a href="/admin?preset=today" className={preset === "today" ? "active" : ""}>Сегодня</a><a href="/admin?preset=yesterday" className={preset === "yesterday" ? "active" : ""}>Вчера</a><a href="/admin?preset=7days" className={preset === "7days" ? "active" : ""}>7 дней</a><a href="/admin?preset=month" className={preset === "month" ? "active" : ""}>Этот месяц</a></div><input type="hidden" name="preset" value="custom" /><label>С <input type="date" name="from" defaultValue={from} required /></label><label>По <input type="date" name="to" defaultValue={to} required /></label><button className="secondary">Показать</button></form>;
}
