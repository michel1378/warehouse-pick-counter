export function PeriodFilter({ preset, from, to, basePath="/admin" }: { preset: string; from: string; to: string; basePath?: string }) {
  return <form className="filters" method="get"><div className="preset-links"><a href={`${basePath}?preset=today`} className={preset === "today" ? "active" : ""}>Сегодня</a><a href={`${basePath}?preset=yesterday`} className={preset === "yesterday" ? "active" : ""}>Вчера</a><a href={`${basePath}?preset=7days`} className={preset === "7days" ? "active" : ""}>7 дней</a><a href={`${basePath}?preset=month`} className={preset === "month" ? "active" : ""}>Этот месяц</a></div><input type="hidden" name="preset" value="custom" /><label>С <input type="date" name="from" defaultValue={from} required /></label><label>По <input type="date" name="to" defaultValue={to} required /></label><button className="secondary">Показать</button></form>;
}
